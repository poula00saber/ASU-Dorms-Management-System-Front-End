# ASU Dorms Management System - Environment Configuration
# ========================================================
# Copy this file to .env.local and update the values.
# .env.local is gitignored and won't be committed.

# ========================================================
# API Base URL Configuration
# ========================================================

# Option 1: Local Development (default)
VITE_API_URL=https://localhost:7152

# Option 2: Cloudflare Tunnel (Quick Tunnel - temporary URL)
# After running the Cloudflare tunnel script, copy your tunnel URL here:
# VITE_API_URL=https://random-words.trycloudflare.com

# Option 3: Cloudflare Tunnel (Named Tunnel - permanent URL)
# If you set up a named tunnel with a custom subdomain:
# VITE_API_URL=https://api.your-domain.com

# Option 4: Production Server
# VITE_API_URL=https://your-production-server.com

# ========================================================
# NOTES:
# ========================================================
# - Always include the protocol (https:// or http://)
# - Never include a trailing slash
# - For Cloudflare tunnels, always use https://
# - The URL should NOT include /api (that's added in the code)
#
# Priority order:
# 1. .env.local (highest - not committed to git)
# 2. .env.development or .env.production (environment-specific)
# 3. .env (lowest - default values)
