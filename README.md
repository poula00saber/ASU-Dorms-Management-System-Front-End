
  # ASU Dorms Management System UI

  This is a code bundle for ASU Dorms Management System UI. The original project is available at https://www.figma.com/design/y1nGq8iN8WTdCgKJOf37Z0/ASU-Dorms-Management-System-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  